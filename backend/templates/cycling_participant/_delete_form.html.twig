<form method="post" action="{{ path('app_cycling_participant_delete', {'id': cycling_participant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cycling_participant.id) }}">
    <button class="btn">Delete</button>
</form>

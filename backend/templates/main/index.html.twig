{% extends 'base.html.twig' %}

{% block title %}RaceHub{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="text-center">
        <a href="{{ path('app_login') }}" class="btn btn-primary btn-lg mx-2">Iniciar Sesión</a>
        <a href="{{ path('app_register') }}" class="btn btn-outline-primary btn-lg mx-2">Registrarse</a>
    </div>
</div>
{% endblock %}

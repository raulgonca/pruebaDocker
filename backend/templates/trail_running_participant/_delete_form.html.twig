<form method="post" action="{{ path('app_trail_running_participant_delete', {'id': trail_running_participant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trail_running_participant.id) }}">
    <button class="btn">Delete</button>
</form>

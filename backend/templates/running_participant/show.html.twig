{% extends 'base.html.twig' %}

{% block title %}RunningParticipant{% endblock %}

{% block body %}
    <h1>RunningParticipant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ running_participant.id }}</td>
            </tr>
            <tr>
                <th>Time</th>
                <td>{{ running_participant.time ? running_participant.time|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Dorsal</th>
                <td>{{ running_participant.dorsal }}</td>
            </tr>
            <tr>
                <th>Banned</th>
                <td>{{ running_participant.banned ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_running_participant_index') }}">back to list</a>

    <a href="{{ path('app_running_participant_edit', {'id': running_participant.id}) }}">edit</a>

    {{ include('running_participant/_delete_form.html.twig') }}
{% endblock %}
